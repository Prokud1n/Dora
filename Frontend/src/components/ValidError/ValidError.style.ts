import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    errorMessage: {
        color: '#FF3826',
        textAlign: 'center',
        fontSize: 12
    }
});

export default styles;
