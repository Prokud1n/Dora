import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    photo: {
        position: 'relative',
        width: 120,
        height: 80,
        borderRadius: 15,
        marginRight: 6,
        marginBottom: 24,
        marginTop: 12
    }
});

export default styles;
