import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    containerHeader: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: 30
    },
    text: {
        fontStyle: 'normal',
        fontWeight: '600',
        fontSize: 17,
        lineHeight: 22,
        marginLeft: 70
    }
});

export default styles;
